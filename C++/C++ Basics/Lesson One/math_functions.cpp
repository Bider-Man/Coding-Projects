#include <iostream>
#include <cmath>

int main() {
    double x = 3;
    double y = 4;
    double z;

    // z = std::max(x, y); - compare and find the higher number
    // z = std::min(x, y); - compare and find the lower number
    // z = pow(x, y); - raise x to the power of y
    // z = sqrt(x) - square root of x.
    // z = abs(x); - Absolute value of x.
    // z = round(x) - round x to 1 sf
    // z = ceil(x) - round x up
    // z = floor(x) - round x down
    std::cout << z;
    return 0;
}
